name: BuildAndTest

permissions:
  contents: write
  checks: write
  pull-requests: write
  packages: write

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize
    branches:
      - main
  merge_group:
    branches:
      - main
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  build_test_ubuntu:
    name: Build and test
    runs-on: ubuntu-latest
    env:
      BUILD_TYPE: Debug
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref || github.ref }}

      - name: License Header Check
        id: license_check
        uses: B1NARY-GR0UP/nwa@main
        continue-on-error: true
        with:
          version: latest
          cmd: check
          path: .nwa-config.yaml

      - name: License Header Auto-fix
        if: steps.license_check.outcome == 'failure' && github.event_name == 'pull_request' && github.actor != 'github-actions[bot]' && github.event.pull_request.head.repo.full_name == github.repository
        uses: B1NARY-GR0UP/nwa@main
        with:
          version: latest
          cmd: add
          path: .nwa-config.yaml

      - name: Commit license header fixes
        id: license_autocommit
        if: steps.license_check.outcome == 'failure' && github.event_name == 'pull_request' && github.actor != 'github-actions[bot]' && github.event.pull_request.head.repo.full_name == github.repository
        run: |
          if git diff --quiet; then
            echo "pushed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore(licenses): auto-apply headers"
          git push origin "HEAD:${{ github.head_ref }}"
          echo "pushed=true" >> "$GITHUB_OUTPUT"

      - name: Fail if license headers invalid
        if: steps.license_check.outcome == 'failure' && steps.license_autocommit.outputs.pushed != 'true'
        run: |
          echo "License header check failed. Run ./scripts/add-licenses-include-h.sh and commit the changes."
          exit 1

      - name: Log in to the Container registry
        if: steps.license_autocommit.outputs.pushed != 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        if: steps.license_autocommit.outputs.pushed != 'true'
        uses: docker/setup-buildx-action@v3

      - name: Build dev image (cached)
        if: steps.license_autocommit.outputs.pushed != 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .devcontainer/Dockerfile
          tags: mldp-pvxs-driver-ci:latest
          cache-from: type=registry,ref=ghcr.io/slaclab/mldp-pvxs-driver:buildcache
          cache-to: type=registry,ref=ghcr.io/slaclab/mldp-pvxs-driver:buildcache,mode=max
          load: true
          build-args: |
            COMPILER=llvm
            PVXS_VERSION=1.4.1

      - name: Set up Docker Compose
        if: steps.license_autocommit.outputs.pushed != 'true'
        uses: docker/setup-compose-action@v1

      - name: Configure, build, test inside dev image
        if: steps.license_autocommit.outputs.pushed != 'true'
        id: build
        run: |
          docker compose -f docker-compose.yml -f docker-compose-test.yml --profile prebuilt up --abort-on-container-exit --exit-code-from ci-prebuilt ci-prebuilt

      - name: Publish test report
        if: always() && steps.license_autocommit.outputs.pushed != 'true'
        uses: dorny/test-reporter@v2
        with:
          name: ctest
          path: build/test-results/junit.xml
          reporter: java-junit

      - name: Upload test artifacts
        if: always() && steps.license_autocommit.outputs.pushed != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            build/test-results/
            build/Testing/
